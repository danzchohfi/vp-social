.env
.env.local
.next/
node_modules/
.vercel/
*.log
*.tsbuildinfo

# Project uses `npm install --legacy-peer-deps` without committing the
# lockfile — drizzle-kit ↔ better-auth peer-dep clash makes the lock
# noisy and Vercel re-resolves on every build anyway.
package-lock.json

# Icons generated by scripts/generate-icons.mjs at prebuild time.
# Source of truth: /logo.png (repo root).
public/icon.png
public/icon-192.png
public/icon-512.png
public/icon-1024.png
public/apple-icon.png
public/favicon-32.png
